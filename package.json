{
  "name": "sms-notifier",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "start": "wrangler dev"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  },
  "dependencies": {
    "@upstash/kafka": "^1.3.3",
    "webcrypto": "^0.1.1"
  }
}
